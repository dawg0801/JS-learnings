const name = "Aritra"
const repoCount = 50

// console.log(name + repoCount + " Value"); never write like this

console.log(`Hello my name is ${name} and my repo count is ${repoCount}`);
// use back-ticks(``) its the modern way of syntax

const gameName = new String("Doom") // declaration of a string
